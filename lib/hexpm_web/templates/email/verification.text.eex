<%
verify_url = url(email_uri(), ~p"/email/verify?username=#{@username}&email=#{@email_address}&key=#{@key}")
%>
Email Verification

<%= Verification.intro() %>

<%= Common.follow_link_instruction(verify_url, :text) %>
