<.auth_layout
  title="Log In"
  subtitle="Use your credentials to log in to your account"
  show_oauth={true}
  oauth_provider="GitHub"
  oauth_href={~p"/auth/github"}
>
  <%!-- Form --%>
  <%= form_tag(~p"/login", id: "login-form", class: "space-y-6") do %>
    <%!-- Username/Email Field --%>
    <.text_input id="username" name="username" label="Username or Email" />

    <%!-- Password Field --%>
    <.password_input id="password" name="password" label="Password">
      <:hint>
        <.text_link href={~p"/password/reset"} variant="primary" class="text-small">
          Forgot Password?
        </.text_link>
      </:hint>
    </.password_input>

    <%!-- Hidden Fields --%>
    <input type="hidden" name="return" value={@return} />

    <%!-- Submit Button --%>
    <.button type="submit" full_width class="mt-8">
      Log In
    </.button>
  <% end %>

  <%!-- Footer Links --%>
  <:footer_links>
    <p class="text-small text-center mt-8">
      <span class="text-grey-800">Don't have an account?</span>
      <.text_link href={~p"/signup"} variant="primary" class="font-semibold ml-1">
        Register Now
      </.text_link>
      |
      <.text_link href={~p"/email/verification"} variant="secondary">
        Resend verification email
      </.text_link>
    </p>
  </:footer_links>
</.auth_layout>
